package anypay

import (
	"testing"
)

func TestCreateApp(t *testing.T) {

	// Define your app
	/*app := db.Apps.CreateOne(
		db.Apps.Name.Set("Test App"),
		db.Apps.AccountID.Set(1),
		db.Apps.PublicKey.Set("pub_key"),
		db.Apps.PrivateKey.Set("priv_key"),
		db.Apps.WebhookURL.Set("https://example.com/webhook"),
	).Exec(context.Background())

	if app.ID == 0 {
		t.Errorf("expected to create an app, but didn't")
	}
	*/
}
